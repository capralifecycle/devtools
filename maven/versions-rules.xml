<?xml version="1.0" encoding="UTF-8"?>
<!-- See https://stackoverflow.com/a/48715568 for details about this file -->
<ruleset xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" comparisonMethod="maven" xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 http://mojo.codehaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">
  <ignoreVersions>
    <!-- Ignore Alpha's, Beta's, release candidates and milestones -->
    <ignoreVersion type="regex">(?i).*Alpha(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*a(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*Beta(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*-B(?:-?[\d\.]+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*-eap(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*RC(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*CR(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*M(?:-?\d+)?</ignoreVersion>

    <!-- To be reviewed -->
    <ignoreVersion type="regex">.*.alpha.*</ignoreVersion>
    <ignoreVersion type="regex">.*.beta</ignoreVersion>
    <ignoreVersion type="regex">.*-b01</ignoreVersion>
    <ignoreVersion type="regex">.*.pr[0-9]</ignoreVersion>
    <ignoreVersion type="regex">.*-eap-.*</ignoreVersion>
    <ignoreVersion type="regex">.*-RC[0-9].*</ignoreVersion>
    <ignoreVersion type="regex">.*.RC[0-9].*</ignoreVersion>
    <ignoreVersion type="regex">.*-rc</ignoreVersion>
    <ignoreVersion type="regex">.*.M[0-9].*</ignoreVersion>
    <ignoreVersion type="regex">.*-SNAP[0-9]+</ignoreVersion>
    <ignoreVersion type="regex">.*-rc[0-9]+</ignoreVersion>
    <ignoreVersion type="regex">.*-rc-[0-9]+</ignoreVersion>
    <ignoreVersion type="regex">.*-beta[0-9]+</ignoreVersion>
    <ignoreVersion type="regex">.*-beta-[0-9]+</ignoreVersion>
    <ignoreVersion type="regex">.*.kotlin12+</ignoreVersion>     
    <ignoreVersion type="regex">.*-native-mt-[0-9]+</ignoreVersion>    
    
    <!-- Ignore .prX from jackson etc. -->
    <ignoreVersion type="regex">(?i).*pr\d+</ignoreVersion>
    <!-- Ignore org.postgresql:postgresql:42.2.5.jre7 and alike -->
    <ignoreVersion type="regex">(?i).*jre\d+</ignoreVersion>
  </ignoreVersions>
</ruleset>
